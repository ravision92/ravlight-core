; RavLight Core — platformio.ini
; env:xdmx_v2_veyron  → XDMX rev2.2 board + Veyron fixture (full build)
;
; To add a new board/fixture:
;   1. Create boards/<your_board>.h with HW_PIN_* and RAVLIGHT_HAS_* defines
;   2. Add a new [env:<board>_<fixture>] section below
;   3. Use -include "boards/<your_board>.h" and the relevant module flags

[env:xdmx_v2_veyron]
platform = espressif32
board = esp32dev
framework = arduino
lib_compat_mode = strict
board_build.partitions = partitions.csv
extra_scripts = pre:flash_all.py
build_flags =
    -DELEGANTOTA_USE_ASYNC_WEBSERVER=1
    -include "boards/xdmx_v2.h"
    -D RAVLIGHT_MODULE_ETHERNET
    -D RAVLIGHT_MODULE_DMX_PHYSICAL
    -D RAVLIGHT_MODULE_RECORDER
    -D RAVLIGHT_MODULE_TEMP
    -D RAVLIGHT_MODULE_RESET
    -D RAVLIGHT_FIXTURE_VEYRON
lib_deps =
    FastLED
    NeoPixelBus
    FS
    SPIFFS
    arduino-libraries/Ethernet @ ^2.0.0
    https://github.com/forkineye/ESPAsyncE131
    https://github.com/mathieucarbou/ESPAsyncWebServer
    https://github.com/adafruit/Adafruit_BusIO
    ayushsharma82/ElegantOTA @ ~3.1.5
    SPI
    Wire
    bblanchon/ArduinoJson @ ^6.20.0
    someweisguy/esp_dmx
    hideakitai/ArtNet @ ^0.8.0
    https://github.com/marian-craciunescu/ESP32Ping

; QuinLED Dig-Octa — reference WiFi-only build (uncomment to enable)
; [env:quinled_octa_8ch]
; platform = espressif32
; board = esp32dev
; framework = arduino
; lib_compat_mode = strict
; board_build.partitions = partitions.csv
; build_flags =
;     -DELEGANTOTA_USE_ASYNC_WEBSERVER=1
;     -include "boards/quinled_octa.h"
;     -D RAVLIGHT_MODULE_RESET
;     -D RAVLIGHT_FIXTURE_QUINLED_OCTA
; lib_deps =
;     FastLED
;     ...
