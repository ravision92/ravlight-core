.pio/
.vscode/
*.pyc
__pycache__/
.claude/
data/config.json
CLAUDE.md
FULL_CHANGELOG
release/
*.bak
# Generated by scripts/gzip_assets.py — rebuilt from data/*.{html,css,js}
data/**/*.gz
# Reference docs / datasheets / scratch dumps — not part of the firmware source
*.pdf
/~/
ravlight-rv1-full.html
Orion_DMX_Guide.md
RavLight_Winch_DMX_Design.md
# Local debug / scratch artefacts — never go in the public repo
.playwright-mcp/
# Browser screenshots land in the repo root, not in .playwright-mcp/, and a
# git add -A once swept 860 KB of them into a firmware commit. Root only: the
# real image assets live under data/.
/*.png
screenshot_*.png
orion_brochure.html
scripts/gen_brochure_pdf.*
# WIP stubs kept local until their feature is wired in
include/nfc.h
src/nfc.cpp
boards/xdmx_v3.h/orion-save-*.png
boards/xdmx_v3.h

# Playwright MCP artefacts (screenshots taken while checking the Polaris UI)
.playwright-mcp/
zones-*.png
