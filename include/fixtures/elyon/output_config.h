#pragma once
// Moved to core/output_config.h (now shared with Orion's LED outputs and any other
// fixture). This forwarding include keeps existing Elyon paths working.
#include "core/output_config.h"
